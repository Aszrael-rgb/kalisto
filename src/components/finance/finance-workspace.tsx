"use client";

import { useState, useTransition } from "react";
import { useRouter } from "next/navigation";

import { createTransaction, updateInvoiceStatus } from "@/actions/finance";
import type { InvoiceStatus, TransactionType } from "@/lib/types/database.types";

type Transaction = { id: string; type: TransactionType; amount: number; category: string; description: string | null; date: string };
type Invoice = { id: string; invoice_number: string; subtotal: number; tax_amount: number; total: number; status: InvoiceStatus; due_date: string };
const invoiceStatuses: InvoiceStatus[] = ["borrador", "emitida", "pagada", "vencida"];

export function FinanceWorkspace({ transactions, invoices }: { transactions: Transaction[]; invoices: Invoice[] }) {
  const router = useRouter(); const [showForm, setShowForm] = useState(false); const [message, setMessage] = useState(""); const [isPending, startTransition] = useTransition();
  const income = transactions.filter((item) => item.type === "ingreso").reduce((sum, item) => sum + Number(item.amount), 0); const expenses = transactions.filter((item) => item.type === "gasto").reduce((sum, item) => sum + Number(item.amount), 0);
  function refresh(action: Promise<{ success: boolean; error?: string }>) { startTransition(async () => { const result = await action; setMessage(result.success ? "Cambios guardados." : result.error ?? "No se pudo guardar."); if (result.success) { setShowForm(false); router.refresh(); } }); }
  return <div className="space-y-8"><div className="grid gap-4 md:grid-cols-3">{[["Ingresos", income], ["Gastos", expenses], ["Balance neto", income - expenses]].map(([label, value]) => <div className="rounded-2xl border border-slate-200 bg-white p-6" key={String(label)}><p className="text-sm text-slate-500">{label}</p><p className="mt-3 text-3xl font-semibold text-slate-950">€{Number(value).toFixed(2)}</p></div>)}</div><div className="flex justify-end"><button className="rounded-xl bg-cyan-300 px-4 py-2.5 text-sm font-semibold" onClick={() => setShowForm(true)} type="button">Nueva transacción</button></div>{message ? <p className="text-sm text-slate-600">{message}</p> : null}<section><h2 className="mb-4 text-xl font-semibold">Transacciones</h2>{transactions.length === 0 ? <div className="rounded-2xl border border-dashed border-slate-300 bg-white p-10 text-center text-slate-500">No hay transacciones. Registra la primera.</div> : <div className="overflow-x-auto rounded-2xl border border-slate-200 bg-white"><table className="w-full min-w-[650px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="px-6 py-4">Fecha</th><th className="px-6 py-4">Categoría</th><th className="px-6 py-4">Tipo</th><th className="px-6 py-4">Importe</th></tr></thead><tbody className="divide-y divide-slate-100">{transactions.map((item) => <tr key={item.id}><td className="px-6 py-4">{item.date}</td><td className="px-6 py-4">{item.category}</td><td className="px-6 py-4">{item.type}</td><td className="px-6 py-4 font-semibold">€{Number(item.amount).toFixed(2)}</td></tr>)}</tbody></table></div>}</section><section><h2 className="mb-4 text-xl font-semibold">Facturas</h2>{invoices.length === 0 ? <div className="rounded-2xl border border-dashed border-slate-300 bg-white p-10 text-center text-slate-500">No hay facturas registradas.</div> : <div className="space-y-3">{invoices.map((invoice) => <div className="flex flex-wrap items-center justify-between gap-4 rounded-2xl border border-slate-200 bg-white p-5" key={invoice.id}><div><p className="font-semibold">{invoice.invoice_number}</p><p className="text-sm text-slate-500">€{Number(invoice.total).toFixed(2)} · vence {invoice.due_date}</p></div><select className="rounded-lg border px-3 py-2 text-sm" disabled={isPending} onChange={(event) => refresh(updateInvoiceStatus(invoice.id, event.target.value as InvoiceStatus))} value={invoice.status}>{invoiceStatuses.map((status) => <option key={status} value={status}>{status}</option>)}</select></div>)}</div>}</section>{showForm ? <div className="fixed inset-0 z-10 flex items-center justify-center bg-slate-950/40 p-6"><form className="w-full max-w-md space-y-4 rounded-2xl bg-white p-6" onSubmit={(event) => { event.preventDefault(); const form = new FormData(event.currentTarget); refresh(createTransaction({ type: form.get("type") as TransactionType, amount: Number(form.get("amount")), category: String(form.get("category")), description: String(form.get("description")) })); }}><h2 className="text-xl font-semibold">Nueva transacción</h2><select className="w-full rounded-lg border p-3" name="type"><option value="ingreso">Ingreso</option><option value="gasto">Gasto</option></select><input className="w-full rounded-lg border p-3" name="amount" min="0.01" placeholder="Importe" step="0.01" type="number" required /><input className="w-full rounded-lg border p-3" name="category" placeholder="Categoría" required /><input className="w-full rounded-lg border p-3" name="description" placeholder="Descripción" /><div className="flex justify-end gap-2"><button className="rounded-lg border px-4 py-2" onClick={() => setShowForm(false)} type="button">Cancelar</button><button className="rounded-lg bg-slate-950 px-4 py-2 text-white" disabled={isPending} type="submit">Guardar</button></div></form></div> : null}</div>;
}
